file(GLOB CHAPTERS "chapter*")
foreach(CHAPTER ${CHAPTERS})
    file(GLOB TASKS "${CHAPTER}/task*.cpp")
    foreach(TASK ${TASKS})
        get_filename_component(TASK_NAME ${TASK} NAME_WE)
        get_filename_component(CHAPTER_NAME ${CHAPTER} NAME)
        add_dvren_executable("${CHAPTER_NAME}_${TASK_NAME}" ${TASK})
    endforeach()
endforeach()